3 Counter Machine interpreter
=============================

This is an OCaml implementation of the Plotkin's 3 Counter Machine

To build:
---------
    $ make

To run:
-------

    $ ./main.byte -input VALUE test.3cm
    where
      VALUE is an integer (value of x)
      test.3cm is a test file

One may use an option "-trace" to trace the execution.
